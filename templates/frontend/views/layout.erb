<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Shaf API<%= @page_title && " | #{@page_title}" %></title>
    <link rel="stylesheet" type="text/css" href="/css/main.css">
  </head>
  <body>
    <div class="main-container">
      <main>
        <%= yield %>
      </main>
      <%= erb :headers, locals: {title: 'Request headers', headers: request_headers} %>
      <%= erb :headers, locals: {title: 'Response headers', headers: response_headers} %>
    </div>
  </body>
</html>
