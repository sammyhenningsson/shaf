class <%= policy_class_name %>
  include HALPresenter::Policy::DSL

  <%= attributes.join("\n  ") %>

  link :self, :up, :'create-form'

  link :edit, :'edit-form', :delete do
    resource&.id && current_user&.id == resource.id
  end

  embed :'create-form'

end
